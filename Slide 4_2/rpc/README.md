# 📄 คู่มือการรันและคำอธิบายโค้ด (Slide 4_2: RPC Sort)

โปรเจกต์นี้สาธิตการใช้ **Remote Procedure Call (RPC)** ของระบบ ONC RPC (Sun RPC) เพื่อทำการเรียงลำดับตัวเลขผ่านเครือข่าย โดยที่ Client เรียกฟังก์ชันจาก Server เสมือนเรียกฟังก์ชันในเครื่องตัวเอง

---

## 🛠 วิธีการ Compile และ Run

### 1. การสร้าง Stub ด้วย rpcgen

ก่อนจะคอมไพล์ เราต้องนำไฟล์ `.x` มาสร้างโค้ดภาษา C (Stubs) ก่อนเสมอ:

```bash
# สำหรับโปรเจกต์ rpc
rpcgen sort.x
```

_คำสั่งนี้จะสร้างไฟล์ `sort.h`, `sort_clnt.c`, `sort_svc.c`, และ `sort_xdr.c` ขึ้นมาโดยอัตโนมัติ_

### 2. การ Compilation

ต้องใช้คำสั่งที่ระบุ Library `tirpc` (Transport Independent RPC) สำหรับระบบ Linux สมัยใหม่:

```bash
# คอมไพล์ฝั่ง Server
gcc sort_proc.c sort_svc.c sort_xdr.c -o server -I /usr/include/tirpc -ltirpc

# คอมไพล์ฝั่ง Client
gcc rsort.c sort_clnt.c sort_xdr.c -o client -I /usr/include/tirpc -ltirpc
```

### 2. การรัน (Execution Steps)

**Terminal 1 (Server):**
เริ่มรัน Server เพื่อรอรับ RPC Request:

```bash
./server
```

**Terminal 2 (Client):**
รัน Client โดยระบุชื่อเครื่อง Server (เช่น localhost) ตามด้วยรายการตัวเลข:

```bash
./client localhost 50 10 30 20
```

---

## 💡 โค้ดนี้กำลังสาธิตอะไร? (What is demonstrated?)

โค้ดชุดนี้สาธิตแนวคิดของ **Distributed Computing** ผ่านเทคโนโลยี RPC:

1.  **Interface Definition (IDL)**: การใช้ไฟล์ `.x` (เช่น `sort.x`) เพื่อกำหนดรูปแบบฟังก์ชันและโครงสร้างข้อมูล
2.  **Stub Generation**: การใช้ `rpcgen` (เครื่องมือสร้างโค้ดอัตโนมัติ) สร้าง Stub ฝั่ง Client และ Server
3.  **XDR (External Data Representation)**: การแปลงข้อมูลให้อยู่ในรูปแบบมาตรฐานเพื่อให้รับส่งระหว่างเครื่องต่างสถาปัตยกรรมได้ (จัดการโดยไฟล์ `*_xdr.c`)
4.  **Network Transparency**: Client สามารถเรียกใช้ `sort_1()` ได้โดยไม่ต้องจัดการเรื่อง Socket เอง

---

## 📝 ใครต้องเขียนไฟล์ไหนบ้าง? (Programmer vs rpcgen)

สำหรับการสอบและการพัฒนา RPC สิ่งสำคัญคือต้องรู้ว่าไฟล์ไหนที่เราต้องเตรียมเอง และไฟล์ไหนที่เครื่องมือสร้างให้:

### 1. ไฟล์ที่ Programmer ต้องเตรียมเอง (Written by you)

- **`sort.x`**: ไฟล์นิยาม Interface (ถือเป็นจุดเริ่มต้นของโปรเจกต์)
- **`rsort.c`**: โค้ดฝั่ง Client ที่เราเขียน Logic การรับ Input และการเรียกฟังก์ชันรีโมท
- **`sort_proc.c`**: โค้ดฝั่ง Server ที่เราเขียน Logic การประมวลผลจริงๆ (เช่น `qsort`)

### 2. ไฟล์ที่ `rpcgen` สร้างให้ (Automated Stubs)

_รันคำสั่ง `rpcgen -C sort.x` แล้วจะได้ไฟล์เหล่านี้มา:_

- **`sort.h`**: Header file ที่รวมนิยามโครงสร้างข้อมูลและ Prototype ของฟังก์ชัน
- **`sort_clnt.c`**: **Client Stub** (ตัวแทนฝั่ง Client ที่ทำหน้าที่ส่งข้อมูลข้าม Network)
- **`sort_svc.c`**: **Server Skeleton** (ตัวรับข้อมูลฝั่ง Server แล้วเรียกฟังก์ชันใน `sort_proc.c`)
- **`sort_xdr.c`**: **XDR Filters** (ตัวแปลงข้อมูลให้อยู่ในรูปแบบมาตรฐานเพื่อรับส่งข้ามเครื่อง)

---

## 🔍 ส่วนประกอบของโค้ด (Code Breakdown)

### 🧱 ไฟล์โครงสร้าง (RPC Structure)

| ไฟล์              | หน้าที่                                                                              |
| :---------------- | :----------------------------------------------------------------------------------- |
| **`sort.x`**      | ไฟล์ IDL ที่กำหนดโปรแกรม (SORTPROG), เวอร์ชัน, และฟังก์ชันที่มีให้เรียก              |
| **`rsort.c`**     | โค้ดฝั่ง Client ที่ทำหน้าที่รับ Input และเรียกใช้ Stub ฟังก์ชัน                      |
| **`sort_proc.c`** | โค้ดฝั่ง Server ที่ประกอบด้วย Logic จริงๆ ในการเรียงลำดับ (`sort_1_svc`)             |
| **`sort_clnt.c`** | Client Stub (สร้างโดย rpcgen) ทำหน้าที่แพ็กข้อมูล (Marshal) ส่งให้ Server            |
| **`sort_svc.c`**  | Server Skeleton (สร้างโดย rpcgen) ทำหน้าที่รับข้อมูลและเรียกฟังก์ชันใน `sort_proc.c` |

---

## ⚠️ จุดที่ควรระวังสำหรับการสอบ

- **Program Number**: ใน `sort.x` ถูกกำหนดเป็น `22855` (ต้องไม่ซ้ำกับบริการอื่นในเครื่อง)
- **Static Variable**: ใน `sort_proc.c` ตัวแปรรับผลลัพธ์ (`ss_res`) ต้องเป็น `static` เพราะ RPC จะคืนค่าเป็น Address (Pointer) หากไม่ใช่ static ข้อมูลจะหายไปเมื่อฟังก์ชันจบ
- **Memory Management**: ฝั่ง Server ต้องมีการจัดการหน่วยความจำ (เช่น `free` และ `malloc`) สำหรับข้อมูลที่จะส่งกลับ เพื่อป้องกัน Memory Leak หรือข้อมูลทับซ้อน
- **TIRPC**: ใน Linux รุ่นใหม่ต้องมี `-ltirpc` เพื่อให้ฟังก์ชัน RPC ทำงานได้
