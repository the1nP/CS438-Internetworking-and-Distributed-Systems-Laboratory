# 📄 คู่มือการรันและคำอธิบายโค้ด (Slide 4_1: TCP Client-Server)

ไฟล์เหล่านี้สาธิตการทำงานของ **TCP Socket Programming** เบื้องต้น โดยใช้โมเดล Request-Response ซึ่งตัวอย่างนี้เป็นการส่งชุดตัวเลขจาก Client ไปให้ Server ทำการเรียงลำดับ (Sort) และส่งกลับคืนมา

---

## 🛠 วิธีการ Compile และ Run

### 1. การ Compilation

เปิด Terminal ในโฟลเดอร์ `tcp` แล้วใช้คำสั่ง:

```bash
# คอมไพล์ฝั่ง Server
gcc tcp_server.c -o server

# คอมไพล์ฝั่ง Client
gcc tcp_client.c -o client
```

### 2. การรัน (Execution Steps)

ต้องรัน Server ก่อนเสมอเพื่อให้รอรับการเชื่อมต่อ:

**Terminal 1 (Server):**

```bash
./server
```

_(Server จะเริ่มเปิด Socket และรอที่ Port 5789)_

**Terminal 2 (Client):**
รัน Client โดยระบุ Hostname (localhost) และ Port (5789):

```bash
./client localhost 5789
```

---

## 💡 โค้ดนี้กำลังสาธิตอะไร? (What is demonstrated?)

โค้ดชุดนี้สาธิตหัวข้อสำคัญในเรื่อง **Network Computing**:

1.  **Connection-Oriented Communication**: การใช้ TCP (SOCK_STREAM) ซึ่งต้องการการสร้างการเชื่อมต่อ (Three-way Handshake) ก่อนส่งข้อมูล
2.  **Socket Lifecycle**: ลำดับการเรียกใช้ฟังก์ชันมาตรฐานของ Socket API
3.  **Data Serialization**: การส่งข้อมูลประเภท `int` และ `array` ผ่านเครือข่าย
4.  **Service Processing**: Server ทำหน้าที่ประมวลผล (ในที่นี้คือการ Sort) แล้วตอบกลับ (Response)

---

## 🔍 ส่วนประกอบของโค้ด (Code Breakdown)

### 🖥 ฝั่ง Server (`tcp_server.c`)

| ขั้นตอน        | ฟังก์ชันที่ใช้       | คำอธิบาย                                                                      |
| :------------- | :------------------- | :---------------------------------------------------------------------------- |
| **1. Create**  | `socket()`           | สร้างจุดเชื่อมต่อ (Socket descriptor)                                         |
| **2. Bind**    | `bind()`             | จอง Port (5789) ให้กับโปรแกรม Server                                          |
| **3. Listen**  | `listen()`           | เปลี่ยน Socket ให้เป็นสถานะ "รอรับการเชื่อมต่อ" (Passive mode)                |
| **4. Accept**  | `accept()`           | **Block** รอจนกว่าจะมี Client ติดต่อเข้ามา เพื่อสร้าง Socket ใหม่สำหรับคุยกัน |
| **5. Process** | `read()` / `qsort()` | อ่านข้อมูลที่ Client ส่งมา แล้วทำการเรียงลำดับ                                |
| **6. Reply**   | `write()`            | ส่งผลลัพธ์ที่เรียงแล้วกลับไปให้ Client                                        |

### 💻 ฝั่ง Client (`tcp_client.c`)

| ขั้นตอน        | ฟังก์ชันที่ใช้    | คำอธิบาย                                              |
| :------------- | :---------------- | :---------------------------------------------------- |
| **1. Setup**   | `gethostbyname()` | เปลี่ยนชื่อ Host (เช่น localhost) เป็น IP Address     |
| **2. Connect** | `connect()`       | เริ่มต้นทำ Connection กับ Server (ส่ง SYN)            |
| **3. Request** | `write()`         | ส่งจำนวนข้อมูล และชุดตัวเลขที่ต้องการให้ Server เรียง |
| **4. Receive** | `read()`          | รอรับผลลัพธ์ที่เรียงเสร็จแล้วจาก Server               |

---

## ⚠️ จุดที่ควรระวังสำหรับการสอบ

- **Port Number**: Server ในโค้ดนี้ Hardcode ไว้ที่ `5789` แต่ Client รับจาก Argument ตัวที่ 2
- **Protocol**: ใน `socket()` ใช้ `SOCK_STREAM` หมายถึง **TCP**
- **Byte Order**: โค้ดนี้ส่ง `int` โดยตรง (อาจมีปัญหาเรื่อง Endianness ในระบบต่างสถาปัตยกรรม แต่ใน localhost จะรันได้ปกติ)
- **Blocking Call**: `accept()` และ `read()` จะรอจนกว่าจะมีเหตุการณ์เกิดขึ้น (Synchronous)
