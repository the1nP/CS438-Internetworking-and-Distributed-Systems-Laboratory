# 📄 คู่มือการรันและคำอธิบายโค้ด (Slide 4_1: TCP Toupper)

ไฟล์เหล่านี้สาธิตการทำงานของ **TCP Socket Programming** โดยตัวอย่างนี้เป็นการส่งข้อความตัวอักษรจาก Client ไปให้ Server เพื่อเปลี่ยนเป็นตัวพิมพ์ใหญ่ (Uppercase) ทั้งหมดแล้วส่งกลับคืนมา

---

## 🛠 วิธีการ Compile และ Run

### 1. การ Compilation

เปิด Terminal ในโฟลเดอร์ `tcp_toupper` แล้วใช้คำสั่ง:

```bash
# คอมไพล์ฝั่ง Server
gcc tcp_server.c -o server

# คอมไพล์ฝั่ง Client
gcc tcp_client.c -o client
```

### 2. การรัน (Execution Steps)

ต้องรัน Server ก่อนเสมอเพื่อให้รอรับข้อความ:

**Terminal 1 (Server):**

```bash
./server
```

_(Server จะเปิด Socket รอที่ Port 5789)_

**Terminal 2 (Client):**
รัน Client โดยระบุ Hostname (localhost) และ Port (5789):

```bash
./client localhost 5789
```

---

## 💡 โค้ดนี้กำลังสาธิตอะไร? (What is demonstrated?)

โค้ดชุดนี้เน้นการสาธิตเรื่อง **Character Processing** ผ่านเครือข่าย:

1.  **Stream Communication**: การใช้ `SOCK_STREAM` (TCP) เพื่อส่งกระแสข้อมูลที่ต่อเนื่องและเชื่อถือได้
2.  **Sequential Data Handling**: Server อ่านข้อมูลทีละตัวอักษร (`read(..., &ch, 1)`) จนเจอเครื่องหมายขึ้นบรรทัดใหม่ (`\n`)
3.  **Application Logic on Server**: การประมวลผลข้อมูลที่ฝั่ง Server (ในที่นี้คือ `toupper()`) ก่อนส่งผลลัพธ์กลับ

---

## 🔍 ส่วนประกอบของโค้ด (Code Breakdown)

### 🖥 ฝั่ง Server (`tcp_server.c`)

| ส่วนสำคัญ                                      | คำอธิบาย                                                                        |
| :--------------------------------------------- | :------------------------------------------------------------------------------ |
| **`socket(AF_INET, SOCK_STREAM, 0)`**          | สร้าง Socket แบบ TCP                                                            |
| **`bind()` & `listen()`**                      | จอง Port 5789 และเริ่มรอการเชื่อมต่อ (Passive Open)                             |
| **`accept()`**                                 | ตอบรับการเชื่อมต่อจาก Client และสร้าง Socket ใหม่ (`sock_2`) สำหรับรับส่งข้อมูล |
| **`do { read(...&ch...) } while(ch != '\n')`** | วนลูปอ่านทีละตัวอักษรจนจบประโยค และเปลี่ยนเป็นตัวพิมพ์ใหญ่ด้วย `toupper(ch)`    |
| **`write(sock_2, str1, len)`**                 | ส่งข้อความที่เปลี่ยนเป็นตัวพิมพ์ใหญ่ทั้งหมดคืนให้ Client ในครั้งเดียว           |

### 💻 ฝั่ง Client (`tcp_client.c`)

| ส่วนสำคัญ                                            | คำอธิบาย                                                         |
| :--------------------------------------------------- | :--------------------------------------------------------------- |
| **`gethostbyname()`**                                | หา IP Address จากชื่อ Host                                       |
| **`connect()`**                                      | ขอเชื่อมต่อไปยัง Server (Active Open)                            |
| **`fgets(str1, 80, stdin)`**                         | รับข้อความจากผู้ใช้งานผ่านคีย์บอร์ด                              |
| **`write()`**                                        | ส่งข้อความไปยัง Server                                           |
| **`do { read(...) putchar(ch) } while(ch != '\n')`** | รออ่านผลลัพธ์จาก Server ทีละตัวอักษรแล้วแสดงผลบนหน้าจอจนจบประโยค |

---

## ⚠️ จุดที่ควรระวังสำหรับการสอบ

- **Termination Character**: ทั้ง Client และ Server ใช้ `\n` (Newline) เป็นตัวบอกจุดสิ้นสุดของข้อความ
- **Protocol Model**: เป็นแบบ **Iterative Server** (ในตัวอย่างนี้จะรับ Client ได้ทีละตัว และทำงานเสร็จแล้วปิดโปรแกรมทันที)
- **Functionality**: ใช้ฟังก์ชันพื้นฐานครบถ้วนตามมาตรฐาน Socket API (Create -> Bind -> Listen -> Accept vs Create -> Connect)
