# 📄 คู่มือการรันและคำอธิบายโค้ด (Slide 5_1: RMI Sort - ass3)

โปรเจกต์นี้สาธิตการใช้ **Java RMI (Remote Method Invocation)** เพื่อสร้างบริการเรียงลำดับตัวเลข ซึ่งเป็นเทคโนโลยีที่ช่วยให้ Java Virtual Machine (JVM) ต่างเครื่องกันสามารถเรียกใช้งานเมธอด (Method) ร่วมกันได้เสมือนอยู่ในเครื่องเดียวกัน

---

## 🛠 วิธีการ Compile และ Run

การรัน RMI จะมีความซับซ้อนกว่า TCP/RPC เล็กน้อย เพราะต้องรัน **RMI Registry** เป็นตัวกลางครับ:

### 1. การ Compilation

คอมไพล์โค้ด Java ทุกไฟล์ (Interface, Server, และ Client):

```bash
javac ass3_interface.java ass3_server.java ass3_client.java
```

### 2. การรัน (Execution Steps)

**Terminal 1 (RMI Registry):**
เปิดตัวกลางสำหรับการจดทะเบียนบริการ (รันในพื้นหลัง):

```bash
rmiregistry &
```

_(หมายเหตุ: หากรันใน VS Code หรือ Terminal ปกติให้เปิดทิ้งไว้ 1 หน้าต่าง)_

**Terminal 2 (Server):**
รัน Server เพื่อลงทะเบียนบริการ โดยระบุไฟล์ Security Policy และใช้เครื่องหมาย **`&`** เพื่อรันเป็น Background Process (ตามที่เห็นในสไลด์):

```bash
java -Djava.security.policy=my.policy ass3_server &
```

_เครื่องหมาย `&` จะทำให้ Server รันอยู่ในพื้นหลัง ทำให้เราสามารถใช้ Terminal เดิมรันคำสั่ง Client ต่อได้ทันที_

_(จะเห็นข้อความ: `Server ready (SortService)`) _

**Terminal 3 (Client):**
รัน Client เพื่อเรียกใช้งาน โดยระบุไฟล์ Policy เช่นเดียวกัน:

```bash
java -Djava.security.policy=my.policy ass3_client localhost
```

_จากนั้นพิมพ์ตัวเลข เช่น `10 5 8 1` แล้วกด Enter_

---

## 🔒 เรื่อง Security Policy (เพิ่มเติมสำหรับการสอบ)

ในการใช้งาน RMI จริง Java มักจะต้องการให้เรากำหนดสิทธิการเข้าถึง (Permission) ผ่านไฟล์ Policy เพื่อความปลอดภัย:

- **Flag**: `-Djava.security.policy=ชื่อไฟล์.policy`
- **เนื้อหาในไฟล์ (ตัวอย่างไฟล์ `my.policy` ที่อนุญาตทั้งหมด)**:
  ```java
  grant {
      permission java.security.AllPermission;
  };
  ```

---

## 📝 ใครต้องเขียนไฟล์ไหนบ้าง? (Programmer's Duty)

ใน Java RMI ยุคปัจจุบัน (Java 5.0+) โปรแกรมเมอร์รับผิดชอบเพียง 3 องค์ประกอบหลัก:

### 1. Remote Interface (`ass3_interface.java`)

- **หน้าที่:** กำหนด "สัญญา" (Contract) ว่าบริการนี้ทำอะไรได้บ้าง
- **จุดสำคัญ:** ต้อง `extends Remote` และทุกเมธอดต้อง `throws RemoteException`

### 2. Server Implementation (`ass3_server.java`)

- **หน้าที่:** เขียน Logic การทำงานจริง (ในที่นี้คือ `Arrays.sort()`)
- **จุดสำคัญ:** ต้องทำการ **Export** ออบเจกต์ (ด้วย `UnicastRemoteObject.exportObject`) และ **Rebind** ชื่อบริการเข้ากับ Registry

### 3. Client Code (`ass3_client.java`)

- **หน้าที่:** ติดต่อไปยัง Registry เพื่อค้นหาบริการ (Lookup) และเรียกใช้งาน
- **จุดสำคัญ:** ใช้ `LocateRegistry.getRegistry()` และ `registry.lookup("ชื่อบริการ")`

---

## 🔍 เจาะลึกโค้ดสำหรับการสอบ (Exam Prep)

| คำศัพท์สำคัญ                            | ความหมาย/หน้าที่                                                                |
| :-------------------------------------- | :------------------------------------------------------------------------------ |
| **`Remote`**                            | Interface พิเศษที่บอก Java ว่าออบเจกต์นี้สามารถถูกเรียกจากเครื่องอื่นได้        |
| **`RemoteException`**                   | Exception ที่ต้องดักจับเสมอ เพราะการสื่อสารผ่านเครือข่ายอาจล้มเหลวได้ทุกเมื่อ   |
| **`RMI Registry`**                      | ตัวกลาง (Directory Service) ที่เก็บเลขที่อยู่ของออบเจกต์ไว้คู่กับชื่อที่ตั้งไว้ |
| **`Stub`**                              | ตัวแทน (Proxy) ฝั่ง Client ที่ทำหน้าที่ห่อหุ้มข้อมูลส่งให้ Server               |
| **`Naming.rebind` / `registry.rebind`** | การจดทะเบียนชื่อบริการเข้ากับ Registry (ถ้าชื่อซ้ำจะทับของเก่าทันที)            |

---

## 💡 สรุปขั้นตอนการทำงาน

1. **Server** สร้างออบเจกต์ -> Export -> จดทะเบียนชื่อใน **Registry**
2. **Client** ติดต่อ **Registry** -> ขอ Stub ของชื่อบริการนั้นมา
3. **Client** เรียกเมธอดผ่าน **Stub** -> ข้อมูลถูกส่งข้าม JVM -> **Server** ประมวลผลจริง -> ส่งผลลัพธ์กลับ

> [!NOTE]
> ใน Java RMI เราไม่ต้องทำ Marshalling/Unmarshalling เองเหมือนใน RPC (XDR) เพราะ Java จัดการการแปลง Object เป็น Byte Stream (Serialization) ให้เราโดยอัตโนมัติครับ!
